<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Show Route</title>
  <link rel="stylesheet" href="/css/style.css"/>
</head>
<body>
  <table>
    <tr>
      <th>Id</th>
      <th>Username</th>
      <th>Email</th>
      <th>Password</th>
    </tr>
    <%for(user of users){%>
    <tr>
        <td><%=user.id%></td>
        <td><%=user.username%></td>
        <td><%=user.email%></td>
        <td><%=user.password%></td>
        <td><form method="GET" action="/user/<%= user.id%>/edit">
          <button>Edit Username</button>
        </form></td>
        <td><form method="post" action="/user/<%= user.id%>/delete?_method=DELETE">
          <button>Delete</button>
        </form></td>
    </tr>
    <%}%>
  </table>
  <form method="get" action="/user/newUSer">
    <button>Create new User</button>
  </form>
</body>
</html>

